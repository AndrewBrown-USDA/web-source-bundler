# 1.1.1 — T1.1-scaffold-and-models

## Goal
Scaffold the `source-bundler` package configuration with Hatchling build backend and define core Pydantic v2 data models for manifests, sources, capture configs, AST documents, and search result items.

## TODO items
- [x] Create `pyproject.toml` configuring the `hatchling` build system, project metadata, required runtime and test dependencies, and console script `source-bundler = "source_bundler.cli:app"`.
- [x] Create `source_bundler/__init__.py` defining `__version__ = "0.1.0"`.
- [x] Create `source_bundler/models.py` with Pydantic v2 models:
  - [x] `CaptureConfig` (viewport width/height defaults 1365x768, locale "en-US", timezone "UTC", user_agent, timeout_seconds 30)
  - [x] `ToolInfo` (name "source-bundler", version "0.1.0")
  - [x] `SourceError` (stage, message, timestamp)
  - [x] `ArtifactRefs` (rendered_html, readable_markdown, ast_json, screenshot, pdf, response_headers, raw_html)
  - [x] `SourceHashes` (dict mapping artifact names to hash strings)
  - [x] `SourceRecord` (source_id, input_url, final_url, title, selected, selection_note, fetched_at, http_status, content_type, artifacts, hashes, errors)
  - [x] `Manifest` (bundle_id, created_at, tool, capture_config, sources)
  - [x] `ASTBlock` (type, level, text, href, items, rows, extra)
  - [x] `ASTDocument` (source_id, title, final_url, blocks)
  - [x] `SearchResultItem` (title, url, snippet)
- [x] Run acceptance validation command: `python -c "import source_bundler; from source_bundler.models import Manifest, ASTDocument, SourceRecord; print('Models OK')"`
- [x] Verify model instantiation and serialization test for `Manifest`, `SourceRecord`, `ASTDocument`, `CaptureConfig`, and `SearchResultItem`.

## Notes
- Analysis confirmed Python 3.14+ environment on Windows.
- Pydantic v2 BaseModel and Field constructs must be used for default values and default factories.
- Constraints honored: Files in scope are strictly `pyproject.toml`, `source_bundler/__init__.py`, and `source_bundler/models.py`. Will not create or touch `utils.py` or `hashing.py`.
- Ensure proper datetime typing and ISO 8601 serializability compatibility.
