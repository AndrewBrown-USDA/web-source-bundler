# 1.4.2 — T4.2-docs-and-validation

## Goal
Write comprehensive documentation in `README.md` covering project overview, installation, CLI usage & options, output artifact structure, extraction pipeline & tradeoffs, provenance & records review, and security & access control considerations, followed by running the complete pytest test suite across all tests.

## TODO items
- [x] Write comprehensive `README.md` covering:
  - [x] Project Overview & Purpose (local CLI tool capturing web sources for AI research workflows).
  - [x] Installation instructions (`pip install -e .` and `python -m playwright install chromium`).
  - [x] CLI Commands & Usage Examples:
    - `source-bundler urls https://example.com https://www.gsa.gov --out ./bundle`
    - `source-bundler file urls.txt --out ./bundle`
    - `source-bundler search-results results.json --out ./bundle --interactive`
    - All option flags: `--no-pdf`, `--no-screenshot`, `--include-raw-html`, `--include-links-table`, `--redact-pattern`, `--timeout`, `--interactive`, `--out` / `-o`, `--version` / `-v`.
  - [x] Output Directory & Artifact Structure (detailed tree showing `manifest.json`, `combined.md`, `checksums.sha256`, `sources/001/` with `metadata.json`, `rendered.html`, `readable.md`, `ast.json`, `screenshot.png`, `page.pdf`, `raw.html`, `response_headers.json`).
  - [x] Content Extraction Pipeline & Tradeoffs (Playwright DOM extraction, boilerplate element stripping, readability/lxml main content targeting, block AST structuring, Markdown conversion, regex redaction, and links table formatting).
  - [x] Provenance & Records Review (deterministic capture configuration: fixed 1365x768 viewport, UTC timezone, en-US locale, static User-Agent, SHA-256 integrity checksums for all files, complete manifest tracking).
  - [x] Security & Access Control Considerations (isolated browser sandbox, untrusted content handling, non-credentialed local execution, no automatic uploads, no paywall or robots bypass, local data privacy).
- [x] Run full test suite validation: `python -m pytest -v`.

## Notes
- Files in scope: `README.md`.
- All 70 unit tests in test suite pass cleanly without live network access.
- Acceptance command: `python -m pytest -v`.

## Questions
None.
