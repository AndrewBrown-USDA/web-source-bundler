# Task 2.1.1: Fix browser download blocking & unify User-Agent strings

## Status: COMPLETED

## Objective
1. Unify default user agent string across `source_bundler/models.py` and `source_bundler/capture.py` to a single constant `DEFAULT_USER_AGENT = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36 (SourceBundler/0.1.0)"` (defined in `models.py` or imported in `capture.py`).
2. Update `CaptureConfig.user_agent` in `source_bundler/models.py` to default to `DEFAULT_USER_AGENT`.
3. In `source_bundler/capture.py`, configure `browser.new_context(accept_downloads=False, ...)` in both the primary try block and fallback to ensure downloads are explicitly blocked.

## Plan
1. In `source_bundler/models.py`:
   - Define `DEFAULT_USER_AGENT = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36 (SourceBundler/0.1.0)"`.
   - Update `CaptureConfig.user_agent: str = DEFAULT_USER_AGENT`.
2. In `source_bundler/capture.py`:
   - Import `DEFAULT_USER_AGENT` from `source_bundler.models`.
   - Add `"accept_downloads": False` to `context_kwargs`.
   - Ensure `browser.new_context` passes `accept_downloads=False` (via `context_kwargs`).
3. Run verification tests:
   - `python -m pytest tests/test_capture.py`
4. Update `planning/STATE_2.md` with completion report under `## 2.1.1`.
