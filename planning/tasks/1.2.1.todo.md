# 1.2.1 — T2.1-selection-and-inputs

## Goal
Implement URL file parsing, search results JSON parsing, URL deduplication, and interactive source selection prompt in `source_bundler/selection.py`, verified with comprehensive unit tests in `tests/test_selection.py`.

## TODO items
- [x] Create `source_bundler/selection.py` with:
  - [x] `parse_url_file(file_path: Union[str, Path]) -> List[SearchResultItem]`: Reads plain text, ignores empty/whitespace-only lines and `#` comments, wraps valid URLs into `SearchResultItem(url=..., title=None, snippet=None)`.
  - [x] `parse_search_json(file_path: Union[str, Path]) -> List[SearchResultItem]`: Reads JSON array from file, parses items supporting `url` or `link` keys for URL, optional `title`, and optional `snippet`.
  - [x] `deduplicate_items(items: List[SearchResultItem]) -> List[SearchResultItem]`: Deduplicates `SearchResultItem` records using `normalize_url(item.url)` while preserving first-seen order.
  - [x] `select_sources_interactively(items: List[SearchResultItem], prompt_func: Optional[Callable[[str], str]] = None) -> List[Tuple[SearchResultItem, bool, Optional[str]]]`:
    - Iterates over items, formatting candidate info: `[{i}] {title}\n  {url}\n  {snippet}\n Include this source? [Y/n]: `
    - Supports default 'Y' / 'y' or empty enter as True; 'N' / 'n' as False.
    - Captures optional selection note (e.g. note or exclusion reason) if provided or structured, returning `List[Tuple[SearchResultItem, bool, Optional[str]]]`.
- [x] Create unit tests in `tests/test_selection.py`:
  - [x] `test_parse_url_file`: valid URLs, comments, empty lines, leading/trailing whitespace, non-existent file handling.
  - [x] `test_parse_search_json`: valid JSON array with `url` and `link`, missing optional fields (`title`, `snippet`), malformed JSON handling, top-level object containing items array fallback if applicable.
  - [x] `test_deduplicate_items`: duplicates with different casing, trailing slashes, order preservation, empty lists.
  - [x] `test_select_sources_interactively`: mock `prompt_func` inputs (`"y"`, `"n"`, `""`, `"Y"`, custom notes) validating selection boolean and returned tuples.
- [x] Run acceptance command: `python -m pytest tests/test_selection.py -v`.

## Notes
- Discovered models: `SearchResultItem` in `source_bundler/models.py` has fields `title: Optional[str] = None`, `url: str`, `snippet: Optional[str] = None`.
- Discovered utils: `normalize_url` is already implemented in `source_bundler/utils.py`.
- Constraints honored: Files touched strictly limited to `source_bundler/selection.py` and `tests/test_selection.py`. Do not touch `extract.py` or `markdown.py`.
- All operations are offline-capable and cross-platform (using `pathlib.Path`).

