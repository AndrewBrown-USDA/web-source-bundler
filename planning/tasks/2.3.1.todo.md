# Task 2.3.1: Align unit tests, verify end-to-end test suite and typing

## Plan
1. Check existing tests in `tests/test_markdown.py`, `tests/test_bundle.py`, `tests/test_capture.py`, `tests/test_cli.py`.
2. Inspect `source_bundler/markdown.py` and `source_bundler/bundle.py` to ensure exact header and formatting assertions match the spec:
   - Header formatting in combined markdown: `Bundle ID: {bundle_id}`, `Tool: {tool_name} {tool_version}` (or `v{tool_version}`), etc.
3. Update test assertions in `tests/test_markdown.py` and `tests/test_bundle.py` if any discrepancies exist with the current implementation / spec.
4. Run `python -m pytest` across all test files to verify 100% test pass.
5. Record results and append completion report in `planning/STATE_2.md`.
