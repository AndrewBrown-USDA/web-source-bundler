# Task 2.2.1 Todo: Refactor extract.py: deduplicate title extraction & clean DOM traversal

- [x] Create plan and verify task scope
- [x] Refactor `extract.py`:
  - [x] Implement `_extract_page_title(soup: BeautifulSoup) -> str` to deduplicate title extraction logic across initial parsing and readability cleanup.
  - [x] Implement `_ast_for_tag(el: Tag, parent_tags: List[str]) -> Optional[ASTBlock]` and modular DOM helpers for headings, paragraphs, lists, blockquotes, code blocks, tables, and links.
  - [x] Update `build_ast_document` to use the modular helpers and clean DOM traversal.
- [x] Run test suite `python -m pytest tests/test_extract.py` and ensure full test suite passes.
- [x] Append completion report to `planning/STATE_2.md` under `## 2.2.1`.
