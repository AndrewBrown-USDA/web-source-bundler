# Task 2.2.2 Plan: Refactor bundle.py: replace `_get_val` with typed domain model access

## Objective
Refactor `BundlePackager.add_source` in `source_bundler/bundle.py`:
1. Remove or replace untyped `_get_val` helper with typed `CaptureResult` attribute accesses (supporting dict fallback gracefully if needed).
2. Cleanly build `ReadableSourceContext` or typed arguments when calling `build_readable_markdown`.
3. Improve type safety and eliminate loose/untyped dict accesses while preserving complete backward compatibility.

## Scope
- `source_bundler/bundle.py`
- `planning/tasks/2.2.2.todo.md`
- `planning/STATE_2.md`

## Step-by-Step Implementation Plan
1. In `source_bundler/bundle.py`:
   - Import `CaptureResult` from `source_bundler.capture` (or define union type with typing/graceful fallback).
   - Import `ReadableSourceContext` from `source_bundler.markdown`.
   - Update `add_source` signature to accept `capture_result: Union[CaptureResult, Dict[str, Any], Any]`.
   - Replace untyped `_get_val` calls with a clean typed extraction strategy:
     - Check if `isinstance(capture_result, CaptureResult)` or convert/access typed attributes directly, with graceful `.get()` fallback if `capture_result` is a `dict`, and `getattr` fallback otherwise.
     - Deprecate or remove `_get_val` helper (or keep a small typed accessor helper if dicts are supported).
   - Construct `ReadableSourceContext` cleanly to pass into `build_readable_markdown(context=...)`.
2. Run targeted test: `python -m pytest tests/test_bundle.py`.
3. Update `planning/STATE_2.md` under `## 2.2.2`.
